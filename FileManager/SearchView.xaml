<Window x:Class="FileManager.SearchView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FileManager"
        mc:Ignorable="d"
        Title="SearchView" Height="373.438" Width="425">
    <Grid>
        <ListView HorizontalAlignment="Left" Height="342" VerticalAlignment="Top" Width="417" RenderTransformOrigin="-0.097,-0.379" Name="liste" MouseRightButtonDown="liste_MouseRightButtonDown">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Nom "
                                    DisplayMemberBinding="{Binding name}"
                                    Width="auto"/>
                    <GridViewColumn Header="Type"
                                    DisplayMemberBinding="{Binding type}"
                                    Width="auto"/>
                    <GridViewColumn Header="Chemin"
                                    DisplayMemberBinding="{Binding path}"
                                    Width="auto"/>

                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>
